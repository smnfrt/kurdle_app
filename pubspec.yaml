name: kurdle_app
description: "Leyar — Kurmancî kelime oyunu. Günlük Wordle, AI ile Scrabble benzeri kelime savaşı, arkadaşlarla multiplayer, 216k+ entry'lik Ferheng sözlük."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+20

environment:
  sdk: ^3.6.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_animator: ^3.2.1
  tuple: ^2.0.0
  share_plus: ^10.0.0
  path_provider: ^2.1.0
  package_info_plus: ^8.0.0
  audioplayers: ^6.6.0
  characters: ^1.3.0
  shared_preferences: ^2.3.0

  # Firebase
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.3
  cloud_firestore: ^5.6.7
  firebase_crashlytics: ^4.3.10
  firebase_analytics: ^11.6.0
  google_sign_in: ^6.2.2
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

  # Ferheng (sözlük) — bundle decompression + offline cache
  archive: ^3.6.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4

# ── Marka iconları ────────────────────────────────────────────────
# Üretim: dart run flutter_launcher_icons
# Ana kaynak: assets/branding/icon-1024.png (tool/gen_brand_icon.py türevleri yeniler)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/branding/icon-1024.png"
  adaptive_icon_background: "#E6EEF2"
  adaptive_icon_foreground: "assets/branding/icon-foreground.png"
  min_sdk_android: 24

# ── Splash ekran ──────────────────────────────────────────────────
# Üretim: dart run flutter_native_splash:create
# Light + dark, Android 12+ destekli.
flutter_native_splash:
  color: "#F4F0D6"
  image: "assets/branding/splash-light.png"
  color_dark: "#F4F0D6"
  image_dark: "assets/branding/splash-dark.png"
  android_12:
    color: "#F4F0D6"
    image: "assets/branding/splash-light.png"
    color_dark: "#F4F0D6"
    image_dark: "assets/branding/splash-dark.png"
  android: true
  ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/answers.txt
    - assets/allowed_guesses.txt
    - assets/stats.json
    - assets/kurdish_dictionary.txt
    - assets/turkish_words.txt
    - assets/sounds/ai_turn.wav
    - assets/sounds/lose.wav
    - assets/sounds/pass_turn.wav
    - assets/sounds/score_up.wav
    - assets/sounds/tile_exchange.wav
    - assets/sounds/tile_pickup.wav
    - assets/sounds/tile_place.wav
    - assets/sounds/tile_return.wav
    - assets/sounds/win.wav
    - assets/sounds/word_invalid.wav
    - assets/sounds/word_valid.wav
    - assets/branding/icon-1024.png
    - assets/ferheng/wordlist.txt.gz
    - assets/ferheng/entries.ndjson.gz
    - assets/ferheng/legacy_meanings.json
    - assets/ferheng/tr_meaning_overrides.json.gz
    - assets/ferheng/categories.json
    - assets/ferheng/ATTRIBUTION.md

  # Markaya özel fontlar artık UYGULAMAYA GÖMÜLÜ (runtime'da gstatic'ten
  # çekilmiyor) — anında yüklenir, offline çalışır, FOUT yok. Inter ve Manrope
  # variable font'lardır; fontWeight değerleri wght eksenine eşlenir.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
